# Forge Local Frontier Benchmark

Public suite: reproducible task inputs without hidden oracle.
Private suite: AES-256-GCM encrypted held-out oracle; only the verifier receives the key.

No competitor-performance claim is allowed without an independently supplied comparable run artifact and attestation.
