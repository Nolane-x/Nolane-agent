const REQUIRED=['readyToShowMs','homeInteractiveMs','routeSwitchMs','rendererMemoryBytes','domNodes','longTasks','idleCpuEstimate'];
export function validateUiPerformanceBaseline(input={}){if(!input.machine||!input.machine.os||!Number(input.machine.ramGb))throw new Error('machine metadata is required');if(input.status==='pending-external')return Object.freeze({...input,windows8GbCertified:false});if(!input.metrics)throw new Error('metrics are required');for(const key of REQUIRED){const value=Number(input.metrics[key]);if(!Number.isFinite(value)||value<=0)throw new Error(`${key} must be positive`)}return Object.freeze({...input,windows8GbCertified:input.machine.os==='win32'&&Number(input.machine.ramGb)===8&&input.independent===true})}
export function createPerformanceObserver(){const samples=[];return Object.freeze({record(name,durationMs){if(!name||!Number.isFinite(durationMs)||durationMs<0)throw new Error('invalid performance sample');samples.push(Object.freeze({name:String(name),durationMs}))},snapshot(){return Object.freeze({samples:Object.freeze([...samples]),longTasks:samples.filter(x=>x.durationMs>50)})}})}
