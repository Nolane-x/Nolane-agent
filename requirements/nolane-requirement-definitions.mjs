export const BRAND_REQUIREMENTS = Object.freeze([
  'Establish Nolane Agent as the single canonical product identity',
  'Rename package, executable, CLI and desktop process surfaces',
  'Replace canonical environment variables with NOLANE_AGENT_* and migrate legacy reads',
  'Rename source, update, Windows, VS Code and evidence artifacts',
  'Remove legacy product copy from renderer, desktop recovery and notifications',
  'Rename SDK packages, HTTP headers and public client symbols',
  'Introduce nolane.* schema namespaces for new artifacts',
  'Migrate SecretStorage, session storage and settings keys without losing user data',
  'Migrate config and data directories with reversible receipts',
  'Preserve immutable historical Forge receipts and verify legacy hashes without rewriting',
  'Rewrite documentation, screenshots, onboarding and examples for Nolane Agent',
  'Rename VS Code commands, views and configuration keys with one-release aliases',
  'Create Nolane Agent alpha, beta and stable update channels',
  'Add a product-surface leakage scanner that fails on unapproved legacy names',
]);

export const UI_REQUIREMENTS = Object.freeze([
  'Freeze capability inventory and enforce complete UI contracts',
  'Capture a machine-labelled Windows 8 GB performance and visual baseline',
  'Create isolated UI v3 build, hashed assets and fail-closed fallback switch',
  'Implement the three-layer Quiet Authority design token system',
  'Build the lightweight persistent AppShell and lazy router',
  'Build the action-oriented Session Sidebar and project switcher',
  'Build Home and Mission Composer with enforceable intent presets',
  'Build incremental Mission Workspace rendering and state slices',
  'Build approval, permission and recovery cards with human-readable impact',
  'Build the context-aware Artifact Dock',
  'Build the Review and Ship shell',
  'Build virtualized diff, hunk decisions and inline review comments',
  'Build verified ship, rollback and checkpoint actions',
  'Build Workroom layout and route continuity',
  'Build lazy Monaco editor host and file tree',
  'Build bounded xterm terminal host, problems and output panels',
  'Build browser and preview artifacts with annotation and snapshots',
  'Build the global Review Queue as an action inbox',
  'Build Projects view with trust and mission summaries',
  'Build basic Settings and performance profile controls',
  'Build the lazy Control Plane shell and deep-link routing',
  'Build Control Plane Operations and Mission State routes',
  'Build Runtime, Sandboxes, Processes and Browser Sessions routes',
  'Build Context, Memory, Retrieval, Conflict and Retention routes',
  'Build Evidence, Receipts, Trace and Verification routes',
  'Build Repository Intelligence and Codebase Knowledge routes',
  'Build Trust, Secrets, Permissions and Security routes',
  'Build Governance, Git, Instructions and Guardrails routes',
  'Build Models, Providers, Skills, Plugins, Autonomy, Labs and Release routes',
  'Meet WCAG 2.2 AA, keyboard, focus, live-region and reduced-motion requirements',
  'Meet responsive desktop layouts from 640 px through 1440 px and above',
  'Pass DOM, memory, idle CPU, long-task, latency and visual release budgets',
]);

export const NOLANE_NATIVE_REQUIREMENTS = Object.freeze([
  'Inventory every NolaneNative archive entry with action, fingerprint and provenance',
  'Isolate legacy external reference material from Nolane production imports',
  'Reimplement the canonical autonomous agent loop in Nolane',
  'Reimplement typed agent state, turn lifecycle and stop conditions',
  'Reimplement provider resolution and fallback policy',
  'Reimplement provider adapters behind Nolane provider contracts',
  'Reimplement the tool registry and capability declaration model',
  'Reimplement governed shell and command execution tools',
  'Reimplement file, patch, search and repository tools',
  'Reimplement web, browser and computer-use tools',
  'Reimplement code execution and notebook-style tools',
  'Reimplement MCP, skill discovery and progressive skill loading',
  'Reimplement durable session persistence and recovery',
  'Reimplement session full-text search and indexing',
  'Reimplement context compression and bounded history',
  'Reimplement cross-session memory and memory providers',
  'Reimplement scoped subagent lifecycle',
  'Reimplement delegation context, authority and result handoff',
  'Reimplement gateway lifecycle and platform registry',
  'Reimplement messaging adapters as independent Nolane plugins',
  'Reimplement cron, scheduled jobs and durable automation',
  'Reimplement plugin and hook lifecycle with quarantine',
  'Replace Python ACP dependency with Nolane native runtime protocol',
  'Reimplement trajectory collection and training export',
  'Reimplement command-line client',
  'Reimplement terminal user interface',
  'Re-specify configuration, defaults and migration rules',
  'Reimplement authentication and provider credential flows',
  'Reimplement image, video and media provider interfaces',
  'Reimplement audio, voice and transcription interfaces',
  'Wire prompt-injection quarantine into every untrusted content ingress',
  'Reimplement secret handling without exposing values to agents',
  'Reimplement sandbox, path and irreversible-action boundaries',
  'Reimplement resource, retry, token and wall-clock budgets',
  'Reimplement runtime telemetry, traces and verification receipts',
  'Provide deterministic offline dependency locks and preflight',
  'Create differential behavior tests without importing NolaneNative runtime code',
  'Pass clean-room source, Windows and update reconstruction',
  'Preserve MIT license, Nous Research attribution and transformation provenance',
  'Retire the bundled NolaneNative archive only after every accepted capability has a Nolane replacement',
]);

export const AUDIT_REQUIREMENTS = Object.freeze([
  'Create a requirement acceptance ledger with explicit not_implemented states',
  'Wire content-ingress quarantine into final provider messages',
  'Certify exact published ZIP bytes in a clean room without Git metadata',
  'Separate evaluator smoke, coding benchmark and competitor benchmark lanes',
  'Support Gitless repository discovery with safe filesystem fallback',
  'Map boundary failures to 400 or 403 without leaking internal details',
  'Lock every evidence citation to a source and test SHA-256',
  'Record route-level authentication, authorization and handler telemetry',
  'Replace source-count claims with observable acceptance criteria',
  'Require hidden compositional tests for coding capability claims',
  'Reject fixture, mock or trivial verifier results from marketing claims',
  'Run provider-real dogfooding on Windows with replayable receipts',
  'Make external dependencies self-diagnosing and reproducible',
  'Detect stale, missing and over-concentrated evidence paths in CI',
  'Track implemented-not-wired capabilities separately from verified behavior',
  'Publish non-claims, external gates and failure evidence with every release',
]);

const SMALL_PROGRAMS = Object.freeze({
  DISTILL: [
    'Collect teacher trajectories with public state, action and effect schemas', 'Verify every trajectory step with an appropriate oracle',
    'Reject trajectories containing hallucination, loops or effectless tool calls', 'Separate localization, tool, planning, recovery and verification datasets',
    'Support offline and on-policy distillation', 'Measure student-teacher divergence at each step',
    'Reduce teacher supervision in high-divergence states', 'Support self-consistent action generation at test time',
    'Support multiple teachers with domain-conditioned trust', 'Reject unsafe or reward-hacking episodes even when terminal tests pass',
    'Require held-out repository promotion', 'Version, hash and rollback every student policy',
  ],
  VERIFY: [
    'Register verifiers with explicit soundness scopes', 'Produce dense turn-level verifiable rewards', 'Maintain an oracle reliability ledger',
    'Rank candidates by verifier vectors before scalar aggregation', 'Use compositional hidden tests', 'Bind mutation-strength receipts',
    'Red-team verifier exploits', 'Keep verifier code and hidden data read-only', 'Resolve cross-verifier disagreement',
    'Attribute reward from expected versus actual effects', 'Train process reward models only from verified labels', 'Calibrate verifier confidence and abstention',
  ],
  RECURSIVE: [
    'Define a sidecar looped policy interface', 'Use fixed-memory recurrent state', 'Implement adaptive halting', 'Detect loop convergence and collapse',
    'Run same-FLOP and same-parameter ablations', 'Gate quantization stability', 'Build recursive graph solver packs',
    'Represent latent reasoning without hidden chain-of-thought storage', 'Implement a dynamic-depth controller', 'Fallback to a non-looped policy',
    'Benchmark out-of-distribution transfer', 'Forbid general-intelligence claims from puzzle-only results',
  ],
  SYMBOLIC: [
    'Induce solvers from verified episodes', 'Define typed DSL and constraint schemas', 'Compile AST and codemod transformations', 'Add SMT and Datalog adapters',
    'Generate property-based tests', 'Record solver soundness and incompleteness', 'Gate solver transfer', 'Check solver composition',
    'Sandbox generated solvers', 'Version, prove provenance and rollback solvers', 'Measure amortized value', 'Fallback to a model when a solver abstains',
  ],
  SPECIALIST: [
    'Register specialist models and capability probes', 'Implement a micro-router', 'Track task and domain-conditioned trust', 'Enforce one-generative-model-at-a-time memory policy',
    'Support lazy load, mmap and pressure unload', 'Distill multi-agent behavior into a single policy', 'Handle specialist disagreement',
    'Use model-specific state serializers', 'Share typed embedding and state schemas', 'Benchmark every specialist independently',
    'Record specialist latency and resource receipts', 'Rollback each specialist independently',
  ],
  PLASTICITY: [
    'Implement non-parametric memory reinforcement', 'Bound adapter updates', 'Measure plasticity by KL, norm and regression budgets', 'Implement fast and slow consolidation',
    'Learn adaptation policies', 'Replay by surprise and forgetting risk', 'Gate forward and backward transfer', 'Rollback negative transfer',
    'Route latent memory experts', 'Keep learning in shadow until promotion', 'Expose memory and adapter lineage to users', 'Freeze the stable core by default',
  ],
  CURRICULUM: [
    'Build real-repository training environments', 'Verify mutation validity', 'Separate bug maker, solver and adversary roles', 'Generate capability-conditioned tasks',
    'Reduce trajectories to verified shortest paths', 'Score trajectory quality by the weakest step', 'Enforce contamination controls and repository splits',
    'Progress difficulty while retaining old tasks', 'Add safety curriculum', 'Add reward-hacking curriculum', 'Track dataset licenses and provenance', 'Create reproducible dataset snapshots',
  ],
  COMPUTE: [
    'Estimate task difficulty and value of computation', 'Allocate context, candidate and reviewer budgets dynamically', 'Implement model cascades', 'Calibrate escalation thresholds',
    'Detect compute saturation', 'Manage speculative branches', 'Govern KV-cache budgets', 'Mutually exclude heavy local resources',
    'Attribute RSS-time to each action', 'Route on the quality-cost Pareto frontier', 'Provide emergency Lite degradation', 'Benchmark same-quality cost',
  ],
});

export const REQUIREMENT_DEFINITIONS = Object.freeze([
  ...BRAND_REQUIREMENTS.map((title, index) => ({ id: `NOL-BRAND-${String(index + 1).padStart(3, '0')}`, group: 'Brand and migration', title })),
  ...UI_REQUIREMENTS.map((title, index) => ({ id: `NOL-UI-${String(index + 1).padStart(3, '0')}`, group: 'UI v3', title })),
  ...NOLANE_NATIVE_REQUIREMENTS.map((title, index) => ({ id: `NOL-NOLANE_NATIVE-${String(index + 1).padStart(3, '0')}`, group: 'NolaneNative clean transformation', title })),
  ...AUDIT_REQUIREMENTS.map((title, index) => ({ id: `NOL-AUDIT-${String(index + 1).padStart(3, '0')}`, group: 'Proof-driven audit', title })),
  ...Object.entries(SMALL_PROGRAMS).flatMap(([program, titles]) => titles.map((title, index) => ({ id: `NOL-SMALL-${program}-${String(index + 1).padStart(2, '0')}`, group: `Small-model ${program.toLowerCase()}`, title }))),
]);
