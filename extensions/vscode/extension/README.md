# Nolane Agent for VS Code

A thin governed client for Nolane Agent 0.7 protocol. It never runs shell commands or edits files directly. Tokens are stored in VS Code SecretStorage. All execution, approval, policy, receipts, and rollback remain in Nolane Agent/Nolane Agent Core.
