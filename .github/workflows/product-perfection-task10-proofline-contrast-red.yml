name: Product Perfection Task 10 Proofline Contrast RED

on:
  push:
    branches: [codex/product-perfection-system-v3]
    paths:
      - 'tests/ui-v3-proofline-runtime-contrast.test.mjs'
      - '.github/workflows/product-perfection-task10-proofline-contrast-red.yml'

permissions:
  contents: read

jobs:
  red:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - run: node --test tests/ui-v3-proofline-runtime-contrast.test.mjs
