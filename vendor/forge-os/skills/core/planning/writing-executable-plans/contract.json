{
  "name": "writing-executable-plans",
  "version": "0.2.0",
  "kind": "core",
  "pack": "planning",
  "domain": null,
  "status": "stable",
  "stages": [
    "planning"
  ],
  "domains": [
    "all"
  ],
  "consumes": [
    "architecture-decision",
    "threat-model"
  ],
  "produces": [
    "execution-plan",
    "acceptance-contracts"
  ],
  "preconditions": [
    "intent.confirmed"
  ],
  "invalidates": [
    "downstream:execution-plan",
    "downstream:acceptance-contracts"
  ],
  "tools": [],
  "assurance": [
    "A0",
    "A1",
    "A2",
    "A3"
  ],
  "conflicts": [],
  "reference": "skills/references/core/planning.md",
  "method": {
    "focus": "produce a dependency-aware sequence of vertical increments that a fresh agent can execute and independently verify",
    "steps": [
      "Map verified requirements and acceptance contracts to concrete files, interfaces, migrations, and tests.",
      "Split work where a reviewer could approve one deliverable and reject the next.",
      "For each task, write a failing test, expected failure, minimal implementation, focused verification, regression command, and commit boundary.",
      "Declare exact consumed and produced interfaces so tasks can be executed out of conversational order.",
      "Run a coverage review against the specification and remove placeholders, speculative work, and inconsistent names."
    ],
    "verification": [
      "Can an agent with no prior chat execute each task?",
      "Does every requirement map to a task and acceptance test?",
      "Does every task end in an independently testable artifact?",
      "Are exact paths, signatures, commands, and expected outputs present?"
    ],
    "evidence": [
      "implementation plan",
      "requirement-to-task matrix",
      "dependency DAG",
      "plan self-review"
    ],
    "traps": [
      "“implement as needed”",
      "horizontal layer tasks with no user value",
      "tests deferred to the end"
    ],
    "source": "flagship"
  },
  "procedure": [
    "Read verified artifacts and acceptance contracts.",
    "Slice work into independently reviewable vertical increments.",
    "Map verified requirements and acceptance contracts to concrete files, interfaces, migrations, and tests.",
    "Split work where a reviewer could approve one deliverable and reject the next.",
    "For each task, write a failing test, expected failure, minimal implementation, focused verification, regression command, and commit boundary.",
    "Declare exact consumed and produced interfaces so tasks can be executed out of conversational order.",
    "Run a coverage review against the specification and remove placeholders, speculative work, and inconsistent names.",
    "Declare exact files, interfaces, dependencies, and test commands.",
    "Sequence tasks by dependency and risk, not convenience.",
    "Attach a reviewer gate and evidence requirement to every task.",
    "Publish an executable plan with no placeholders."
  ],
  "gate": {
    "reviewerRole": "independent-reviewer",
    "rules": [
      "The output directly and completely performs writing executable plans within its declared boundary.",
      "Can an agent with no prior chat execute each task?",
      "Does every requirement map to a task and acceptance test?",
      "Does every task end in an independently testable artifact?",
      "Are exact paths, signatures, commands, and expected outputs present?",
      "Every material claim is traceable to an input, decision, executable check, or evidence item.",
      "Required fields are complete and machine-readable.",
      "The producing agent is not the approving reviewer.",
      "Open uncertainty and residual risk are explicit; critical findings are never hidden by an aggregate score."
    ],
    "passCondition": "All mandatory rules pass, evidence targets the current artifact hash, and no unresolved critical finding applies."
  },
  "handoff": {
    "next": {
      "mode": "graph-router",
      "outputTypes": [
        "execution-plan",
        "acceptance-contracts"
      ]
    },
    "requiredEvidence": [
      "contract-validation",
      "independent-review",
      "implementation plan",
      "requirement-to-task matrix",
      "dependency DAG",
      "plan self-review"
    ],
    "requiredFields": [
      "artifactId",
      "schemaVersion",
      "sha256",
      "producingSkill",
      "producingAgent",
      "consumedArtifacts",
      "decisionIds",
      "evidenceIds",
      "residualRisks",
      "validationState",
      "invalidationTargets",
      "stopCondition"
    ],
    "stopWhen": "Output contract is satisfied, a blocker is recorded, or a material human decision is required."
  },
  "context": {
    "estimatedTokens": 1710,
    "maxArtifacts": 8,
    "maxReferenceDepth": 1,
    "verbosity": "domain-precise"
  },
  "failureModes": [
    "guessing a material requirement",
    "producing prose without the contracted artifact",
    "self-approving the output",
    "expanding scope without a decision record",
    "“implement as needed”",
    "horizontal layer tasks with no user value",
    "tests deferred to the end"
  ],
  "optionalConsumes": [
    "product-definition",
    "domain-blueprint"
  ],
  "requiredTools": [],
  "optionalTools": [],
  "description": "Use when writing executable plans is required during planning work, especially when the result must be traceable, independently reviewable, and safe to hand to another agent."
}
