# Failure Modes

- Treating shared headings as semantic duplication.
- Keeping aliases as independent skills to inflate counts.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
