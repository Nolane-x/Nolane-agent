# Failure Modes

- Invalidating the whole project for a local change.
- Deleting old evidence instead of marking it stale.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
