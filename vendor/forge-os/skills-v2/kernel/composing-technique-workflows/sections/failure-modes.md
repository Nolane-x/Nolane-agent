# Failure Modes

- Turning every dependency into a sequential step.
- Hiding optional work inside a mandatory node.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
