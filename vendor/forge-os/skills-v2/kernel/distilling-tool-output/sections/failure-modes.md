# Failure Modes

- Keeping only the final line of a multi-cause failure.
- Letting a summary replace the raw evidence artifact.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
