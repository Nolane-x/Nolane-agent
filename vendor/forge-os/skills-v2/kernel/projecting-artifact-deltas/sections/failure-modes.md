# Failure Modes

- Sending the complete history for one changed field.
- Using timestamps instead of content identity.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
