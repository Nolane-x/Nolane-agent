# Failure Modes

- Loading all references for routine work.
- Selecting examples instead of the required verification contract.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
