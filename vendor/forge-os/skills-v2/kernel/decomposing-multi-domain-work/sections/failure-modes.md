# Failure Modes

- Splitting by arbitrary file count.
- Creating one giant specification for unrelated subsystems.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
