# Failure Modes

- Parallelizing coupled edits to the same invariant.
- Sharing one giant context between all workers.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
