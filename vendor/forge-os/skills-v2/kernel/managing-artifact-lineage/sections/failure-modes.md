# Failure Modes

- Treating hash as protection when metadata is outside the hash.
- Leaving verified descendants active after supersession.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
