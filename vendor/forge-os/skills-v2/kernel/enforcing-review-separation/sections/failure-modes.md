# Failure Modes

- Comparing display names instead of authenticated principals.
- Assuming two service accounts are independent.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
