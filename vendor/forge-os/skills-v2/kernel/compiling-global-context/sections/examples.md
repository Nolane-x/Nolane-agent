# Evaluation Scenarios

## Positive
A task exhibits the trigger conditions in the manifest. The technique produces **context-pack** with current evidence and a bounded context receipt.

## Anti-trigger
A request matches vocabulary but also matches an anti-trigger. The router excludes this technique and records the reason instead of activating it.
