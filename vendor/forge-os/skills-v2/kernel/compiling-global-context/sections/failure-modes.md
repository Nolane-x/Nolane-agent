# Failure Modes

- Budgeting only skill text.
- Truncating the tail without knowing which evidence was removed.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
