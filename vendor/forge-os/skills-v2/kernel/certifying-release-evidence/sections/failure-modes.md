# Failure Modes

- Signing a report that does not bind archive bytes.
- Calling a docs-only adapter certified.

A failed deterministic check is a blocker and cannot be averaged away by semantic quality scores.
