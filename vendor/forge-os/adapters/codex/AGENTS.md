# ForgeOS for Codex

Use `skills/core/kernel/using-forge-os/SKILL.md` as the entry point. Preserve artifact lineage, use tests before implementation, and require independent evidence before advancing gates.
