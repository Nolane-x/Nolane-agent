[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nolane-agent-sdk"
version = "0.0.1"
description = "Python SDK for the Nolane Agent operating plane"
requires-python = ">=3.10"
license = {text = "MIT"}
