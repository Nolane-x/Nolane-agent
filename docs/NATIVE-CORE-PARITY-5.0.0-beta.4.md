# Nolane Native Core Parity — 5.0.0-beta.4

The clean-room inventory tracks **2,110 upstream source/config candidate paths**. These paths are grouped into **65 behavioral contracts**, not counted as one feature per source file.

## Current conformance

- **39 verified contracts**
- **26 external contracts**
- **370 verified upstream paths**
- 0 partial contracts
- 0 implemented-not-wired contracts
- 0 not-implemented contracts
- 0 unmapped paths

Beta.4 locally verifies Agent Behavior Runtime, Session Lifecycle Runtime, Tool Governance Runtime, Profile Configuration Runtime and OAuth Security Runtime. Each has direct tests, negative tests and production wiring.

External status still applies to provider-real adapters, remote infrastructure, platform credentials, browser/desktop journeys, media providers, observability sinks, Windows installation and accessibility certification.

The legacy external runtime and archive remain absent; historical source hashes are evidence only.

`completeParityClaimAllowed=false`

`superiorityClaimAllowed=false`
