# Forge Studio 2.29.0 — Verification Report

Verification requires direct unit tests, DecisionPlane integration tests, two release verifiers, the complete Node suite, all historical frontier gates, ForgeOS validation, SDK tests, Windows bootstrap, source reconstruction and archive integrity.

The world-model gate checks model metadata/selection, foresight economics, bounded rollout horizon/count, reliability pruning, decision delta, real-probe fallback, environment cache invalidation, observed validation and no-commit claims.

The developmental-learning gate checks rejection of unverified outcomes, capability/limit tracking, tool trust, responsibility, ZPD goal selection, novelty blocking, held-out transfer, regression, future-self, rollback and production-promotion denial.

Final counts and receipts are recorded in the Full Release Matrix and `docs/feature-audit-2.29.0.json`.
