# Nolane Agent 5.0.0-beta.3 — Limitations

Beta.3 converts additional shared runtime behavior into Nolane-native code, but it does not prove universal provider, platform or GUI parity.

Authoritative V5 evidence: `docs/feature-audit-5.0.0-beta.3.json` and `docs/REMAINING-GAPS-5.0.0-beta.3.md`.

The Native Core Conformance Catalog contains **61 behavioral contracts**, of which **34 verified contracts** are locally proven and **27 external contracts** require external evidence. It tracks **2,110 upstream source/config candidate paths**, including **240 verified upstream paths** and 1,870 paths awaiting external certification.

Provider-real transports, messenger credentials, real browser/computer-use journeys, Windows installer execution, Windows 8 GB performance, screen-reader testing, responsive visual regression and independent competitor benchmarks remain external.

The legacy external runtime and archive remain absent from production and release packages. Historical attribution and audit inventory are retained as non-executable evidence.

`completeParityClaimAllowed=false`

`superiorityClaimAllowed=false`
