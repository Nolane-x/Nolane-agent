# Nolane Agent 5.0.0-alpha.3 — Remaining Gaps Report

- Tổng yêu cầu: **198**
- Còn lại: **86**
- Hoàn thành một phần: **0**
- Cổng bên ngoài: **0**
- Chưa triển khai: **86**
- Audit SHA-256: `f7a91968368dc1abecee7c6a868ed0725baa285729f755f1812298e276c73586`
- Receipt SHA-256: `8f21c9d15fc9bc87d7ea22f3159fc3f370ae0426e1c014e6ca9a72a893528f5e`

> Báo cáo này liệt kê toàn bộ item chưa ở trạng thái source + test. Không có item nào bị ẩn hoặc gộp mất.

## 1. Brand and migration

### NOL-BRAND-011 — Rewrite documentation, screenshots, onboarding and examples for Nolane Agent

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 2. UI v3

### NOL-UI-002 — Capture a machine-labelled Windows 8 GB performance and visual baseline

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-UI-030 — Meet WCAG 2.2 AA, keyboard, focus, live-region and reduced-motion requirements

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-UI-031 — Meet responsive desktop layouts from 640 px through 1440 px and above

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-UI-032 — Pass DOM, memory, idle CPU, long-task, latency and visual release budgets

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 3. NolaneNative clean transformation

### NOL-NOLANE_NATIVE-010 — Reimplement web, browser and computer-use tools

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-011 — Reimplement code execution and notebook-style tools

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-016 — Reimplement cross-session memory and memory providers

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-025 — Reimplement command-line client

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-026 — Reimplement terminal user interface

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-027 — Re-specify configuration, defaults and migration rules

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-028 — Reimplement authentication and provider credential flows

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-029 — Reimplement image, video and media provider interfaces

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-030 — Reimplement audio, voice and transcription interfaces

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-032 — Reimplement secret handling without exposing values to agents

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-033 — Reimplement sandbox, path and irreversible-action boundaries

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-037 — Create differential behavior tests without importing NolaneNative runtime code

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-038 — Pass clean-room source, Windows and update reconstruction

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-039 — Preserve MIT license, Nous Research attribution and transformation provenance

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-NOLANE_NATIVE-040 — Retire the bundled NolaneNative archive only after every accepted capability has a Nolane replacement

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 4. Proof-driven audit

### NOL-AUDIT-012 — Run provider-real dogfooding on Windows with replayable receipts

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-AUDIT-013 — Make external dependencies self-diagnosing and reproducible

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 5. Small-model distill

### NOL-SMALL-DISTILL-02 — Verify every trajectory step with an appropriate oracle

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-03 — Reject trajectories containing hallucination, loops or effectless tool calls

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-05 — Support offline and on-policy distillation

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-07 — Reduce teacher supervision in high-divergence states

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-08 — Support self-consistent action generation at test time

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-09 — Support multiple teachers with domain-conditioned trust

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-DISTILL-11 — Require held-out repository promotion

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 6. Small-model verify

### NOL-SMALL-VERIFY-05 — Use compositional hidden tests

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-VERIFY-07 — Red-team verifier exploits

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 7. Small-model recursive

### NOL-SMALL-RECURSIVE-01 — Define a sidecar looped policy interface

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-02 — Use fixed-memory recurrent state

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-03 — Implement adaptive halting

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-04 — Detect loop convergence and collapse

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-05 — Run same-FLOP and same-parameter ablations

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-06 — Gate quantization stability

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-07 — Build recursive graph solver packs

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-08 — Represent latent reasoning without hidden chain-of-thought storage

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-09 — Implement a dynamic-depth controller

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-10 — Fallback to a non-looped policy

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-11 — Benchmark out-of-distribution transfer

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-RECURSIVE-12 — Forbid general-intelligence claims from puzzle-only results

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 8. Small-model symbolic

### NOL-SMALL-SYMBOLIC-01 — Induce solvers from verified episodes

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-02 — Define typed DSL and constraint schemas

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-03 — Compile AST and codemod transformations

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-04 — Add SMT and Datalog adapters

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-05 — Generate property-based tests

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-06 — Record solver soundness and incompleteness

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-07 — Gate solver transfer

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-08 — Check solver composition

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-09 — Sandbox generated solvers

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-10 — Version, prove provenance and rollback solvers

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-11 — Measure amortized value

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SYMBOLIC-12 — Fallback to a model when a solver abstains

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 9. Small-model specialist

### NOL-SMALL-SPECIALIST-03 — Track task and domain-conditioned trust

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SPECIALIST-05 — Support lazy load, mmap and pressure unload

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SPECIALIST-06 — Distill multi-agent behavior into a single policy

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SPECIALIST-09 — Share typed embedding and state schemas

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-SPECIALIST-10 — Benchmark every specialist independently

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 10. Small-model plasticity

### NOL-SMALL-PLASTICITY-01 — Implement non-parametric memory reinforcement

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-02 — Bound adapter updates

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-03 — Measure plasticity by KL, norm and regression budgets

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-04 — Implement fast and slow consolidation

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-05 — Learn adaptation policies

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-06 — Replay by surprise and forgetting risk

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-07 — Gate forward and backward transfer

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-08 — Rollback negative transfer

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-09 — Route latent memory experts

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-10 — Keep learning in shadow until promotion

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-11 — Expose memory and adapter lineage to users

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-PLASTICITY-12 — Freeze the stable core by default

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 11. Small-model curriculum

### NOL-SMALL-CURRICULUM-01 — Build real-repository training environments

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-02 — Verify mutation validity

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-03 — Separate bug maker, solver and adversary roles

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-04 — Generate capability-conditioned tasks

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-05 — Reduce trajectories to verified shortest paths

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-06 — Score trajectory quality by the weakest step

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-07 — Enforce contamination controls and repository splits

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-08 — Progress difficulty while retaining old tasks

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-09 — Add safety curriculum

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-10 — Add reward-hacking curriculum

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-11 — Track dataset licenses and provenance

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-CURRICULUM-12 — Create reproducible dataset snapshots

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

## 12. Small-model compute

### NOL-SMALL-COMPUTE-04 — Calibrate escalation thresholds

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có

### NOL-SMALL-COMPUTE-12 — Benchmark same-quality cost

- **Trạng thái:** `not_implemented`
- **Vì sao chưa hoàn tất:** Chưa có implementation + proof đầy đủ trong acceptance ledger.
- **Điều kiện hoàn tất:** Xây implementation trong source, nối vào runtime/API hoặc UI thật, thêm kiểm thử trực tiếp và chạy lại full release matrix.
- **Bằng chứng hiện có:** Chưa có
