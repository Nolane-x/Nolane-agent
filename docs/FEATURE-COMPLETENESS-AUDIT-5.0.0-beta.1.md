# Nolane Agent 5.0.0-beta.1 — Feature Completeness Audit

The canonical registry contains 198 requirements. Beta.1 classifies 193 as `verified_source_test` and 5 as `not_implemented`.

`verified_source_test` means the item has a production entrypoint, exact test, current evidence hashes and a replay receipt. It does not mean independent third-party certification, provider-real autonomy or model superiority. Source-file count is never accepted as proof.

legacy external executable code, archive, runtime routes and packaged payloads are retired. Historical MIT attribution and the immutable 8,548-entry transformation ledger remain documentation and evidence only.

The remaining five items are external Windows performance, runtime/manual UI certification and provider-real Windows dogfooding. They remain visible in the generated gaps report.
