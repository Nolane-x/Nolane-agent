# Forge Studio 2.22.0 release

## Polyglot Runtime Intelligence

This release adds an explicit language capability matrix, Tree-sitter pack governance, pooled LSP contracts, graph fusion, runtime observation evidence, source classification and architecture drift detection.

### Added

- Per-language parser/LSP/build/test/runtime/graph status with evidence and no silent parity claim.
- LSP hover, rename, type definition and diagnostics plus bounded session reuse and idle eviction.
- Relationship edges with provenance, confidence and ambiguity instead of guessed targets.
- Permission-bound runtime observation receipts with secret redaction and bounded retention.
- Architecture Drift Sentinel for cycles, layer violations, duplicate logic, internal-boundary imports and generated/build edits.
- One lazy Polyglot Intelligence Plane inside Repository Intelligence Fabric.
- Required Full Release Matrix gate `polyglot-runtime-intelligence`.
