# Nolane Native Core Parity — 5.0.0-beta.3

The clean-room inventory tracks **2,110 upstream source/config candidate paths**. These paths are grouped into **61 behavioral contracts**, not counted as one feature per source file.

## Current conformance

- **34 verified contracts**
- **27 external contracts**
- **240 verified upstream paths**
- 1,870 external upstream paths
- 0 partial contracts
- 0 implemented-not-wired contracts
- 0 not-implemented contracts
- 0 unmapped paths

Beta.3 locally verifies ACP streaming, provider protocol normalization, repository intelligence, delegation context, browser/search registry behavior, gateway adapter lifecycle, shared command surfaces and usage observability. Each has a direct test, negative test and production wiring.

External status still applies to provider-real adapters, remote infrastructure, platform credentials, browser/desktop journeys, media providers, observability sinks, Windows installation and accessibility certification.

The legacy external runtime and archive remain absent; historical source hashes are evidence only.

`completeParityClaimAllowed=false`

`superiorityClaimAllowed=false`
