# Forge Studio 2.28.0 — Verification Report

The new required release gates are `security-resilience-supply-chain` and `comparative-certification-harness`.

The first proves typed taint blocking, contextual injection and prompt quarantine, dependency/SBOM/integrity decisions, secret exfiltration defense, mission token revocation, audit tamper detection, protected-boundary authorization, bounded sandbox-escape fixtures, failure recovery and privacy-safe HTTP/web/VS Code evidence surfaces.

The second proves benchmark schema/environment/model/budget/permission locking, contamination detection, bounded raw-evidence hashes, failure taxonomy, resource/correction/keep-rate statistics, independent attestation digest binding and fake-provider exclusion. It also proves that external comparative claims remain locked because real competitor runs and trusted independent superiority attestation are absent.

Production boundaries are recorded in `docs/LIMITATIONS-2.28.0.md`. Full release-matrix results are generated under `release/full-release-matrix-2.28.0.*` during packaging.
