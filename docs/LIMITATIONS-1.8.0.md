# Forge Studio 1.8.0 — remaining limits

Date: 2026-07-29

The item-level source of truth is `feature-audit-1.8.0.json`. The exhaustive open-item report is `REMAINING-GAPS-1.8.0.md`.

## Instruction-policy boundary

- Forge does not infer deterministic contradictions from arbitrary natural-language prose. Only typed frontmatter rules participate in conflict resolution.
- Imported documents are guidance only and cannot execute scripts or increase security authority.
- Project instructions remain unavailable until Workspace Trust is explicitly granted.
- Global instruction roots are opt-in configuration and are not inferred from arbitrary user directories.
- Repository guidance cannot modify Forge security policy, capability grants, trust state, audit, or release gates.

## Remaining local and external gaps

The exact list and completion condition for every open requirement is generated in `REMAINING-GAPS-1.8.0.md`. Native OS isolation parity, hosted-provider conformance, production cloud/identity operation, public signing/notarization, and independent comparative benchmarks remain evidence-bound external gates where applicable.
